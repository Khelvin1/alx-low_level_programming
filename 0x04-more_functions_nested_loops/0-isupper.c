#include "main.h"

/**
 * isupper - Check if a letter is upper
 * @X: The number to be checked
 * Return: 1 for upper letter of 0 for my else
 */

int _isupper (int x)

{

	if (x >= 65 && x <= 90
	{
	return (1);
	}
	return (0);
}
